# GoalPilot agent instructions

Use this authority order: the active user request, `docs/PRD.md`, `docs/CONVENTIONS.md`,
`docs/STACK.md`, `docs/TASKS.md`, `docs/MVP_PROJECT_REPORT.md`, then code and comments.

The active build is local-first. Do not deploy or expand AWS infrastructure until the hard gate in
`docs/TASKS.md` is verified. The MVP is an educational simulation: never add real money movement,
live rates, financial-provider credentials, personalized advice, or language implying custody.

Run focused tests with each vertical slice and never mark work complete without command evidence.
